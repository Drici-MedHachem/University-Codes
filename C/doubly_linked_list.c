#include<stdio.h>
#include<stdlib.h>

typedef struct node {
	int key;
	struct node * next;
	struct node * prec;
}node;



node * init_dlist(void) {
	node * head = malloc(sizeof(*head));
	node * z = malloc(sizeof(*z));
	head-> key = 0;
	head->next = 
	return head;
}


int main(void) {
	head = init_dlist
	
	return 0;
}
